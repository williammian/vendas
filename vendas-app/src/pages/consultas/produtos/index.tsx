import { ListagemProdutos, RotaAutenticada } from 'components'

export default function ListagemProdutosPage() {
    return(
        <RotaAutenticada>
            <ListagemProdutos />
        </RotaAutenticada>
    )
}