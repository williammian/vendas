import { RelatorioVendas, RotaAutenticada } from 'components'

export default function RelatorioVendasPage(){
    return (
        <RotaAutenticada>
            <RelatorioVendas />
        </RotaAutenticada>
    )
};